{
  "name": "examen_2_bbdd",
  "version": "1.0.0",
  "description": "1. Inicia un proyecto de NodeJS, instala `express` y crea un archivo `index.js` vacío.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon --watch '*.html' --watch '*.css' --watch '*.js' --watch '*.json' --watch '*.njk' index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
